# SpendWise Global Environment Variables Template (Variable Names Only)

# Backend (Render)
PORT=
NODE_ENV=
DATABASE_URL=
JWT_SECRET=
JWT_EXPIRES_IN=
REFRESH_TOKEN_EXPIRES_DAYS=
FRONTEND_URL=

# Frontend (Vercel)
VITE_API_BASE_URL=
